import { ReactNode } from "react";

export const metadata = {
  title: "FAQ - Tus preguntas respondidas",
  description:
    "Encuentra respuestas a las preguntas más frecuentes sobre nuestra plataforma y servicios.",
};

export default function FaqLayout({ children }: { children: ReactNode }) {
  return children;
}
