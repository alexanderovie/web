import { ReactNode } from "react";

export const metadata = {
  title: "Sobre nosotros - Fascinante Digital",
  description:
    "Conoce más sobre nuestro equipo, valores y socios en Fascinante Digital.",
};

export default function AboutLayout({ children }: { children: ReactNode }) {
  return children;
}
